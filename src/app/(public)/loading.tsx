import SiteLoader from "@/components/Loader";


export default function Loading() {
    // Ce composant sera automatiquement utilisé par Next.js comme fallback de Suspense
    return <SiteLoader />;
}